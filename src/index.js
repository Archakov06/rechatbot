import ChatBot from './ChatBot';
import Loading from './components/Loading';

import { mergeMessage, serializeUserAnswer } from './utils';

export { Loading, mergeMessage, serializeUserAnswer };

export default ChatBot;
